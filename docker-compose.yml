version: "3.1"

services:
  db:
    image: postgres
    restart: always
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=backend
      - POSTGRES_PASSWORD=1234
    volumes:
      - postgres:/var/lib/postgresql

volumes:
  postgres:
